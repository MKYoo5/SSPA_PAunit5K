# 검토의견 — `2.1StsBitInCall` 시트 vs `PAunit_Firm작업_계획서_0921.md`

**검토일**: 2026-09-21
**검토 대상**: `26_PAunit_Coding작업_0921_2.xlsx`의 `2.1StsBitInCall` 시트 ↔ `PAunit_Firm작업_계획서_0921.md`
**교차 확인**: `2.3_Anl_Process`, `2.2.1SysCMDcode`, `Claude결정사항` 시트

---

## 1. 확인된 일치 사항 (문제 없음)

- **`PAnal(3) → DC Total Volt` 매핑**: `2.3_Anl_Process`의 실제 Proc NO 표 기준 DC Total Volt는 Proc NO=4(n=3)가 맞음. `2.1StsBitInCall`의 `E40: PAnal(3) > 400 = PaSTBstsO`는 정확함. (처음엔 ADC 소스별 순서만 보고 오프바이원을 의심했으나, MUX 소스인 Heatsink Temp가 Proc NO=3에 끼어 있어 실제로는 문제 없음이 확인됨.)
- **`PAnal(0) > 10 → PaRFonStsO`**: Proc NO=1(FWD)과 일치, MD 6-12절 수식과 부합.
- **EEPROM `Pary(g,1..34)` 표기가 아직 0-base로 안 바뀐 점**: `2.2.1SysCMDcode` 확인 결과 여전히 `Pary(0,1)~Pary(0,34)`(1-base)이나, 이는 MD 4절에서 이미 "사용자 갱신 시 0부터로 정리" 대상으로 명시된 기존 보류 항목과 일치함. 새로운 문제 아님.

---

## 2. 발견된 실제 버그 — "DC_Pack2 Comm Alm" 중복 오기

`2.1StsBitInCall`의 Comm Alarm 3항목(Pack1/Pack2/Pack3) 표기가 **4곳 모두에서 동일하게 틀려 있음**. `.2` 비트(Pack3이어야 함)가 전부 `Pack2`로 잘못 복사됨.

| 위치 | 현재 표기 (0/1/2번째) |
|---|---|
| 비공식 메모 (E77~79) | `DC_Pack1`, `DC_Pack2`, `DC_Pack2`(중복); G열도 3개 모두 `DCpk1ComAlm: H`로 고정 |
| Latch3 표 (row 141~143, `PA_Sts(4).0~2`) | `DC_Pack1`, `DC_Pack2`, `DC_Pack2`(중복) |
| ENB3 표 (row 195~197, `PA_Sts(7).0~2`) | `DC_Pack1`, `DC_Pack2`, `DC_Pack2`(중복) |
| Unlatch3 표 (row 247~249, `PA_Sts(10).0~2`) | `DC_Pack1`, `DC_Pack2`, `DC_Pack2`(중복) |

MD 5-9절의 `PA_Sts(4).0/.1/.2 = Pack1/2/3 Latch` 정의와 어긋남. 같은 실수가 4곳에 반복된 것으로 보아 원본 행 복사 후 항목명 수정 누락으로 추정됨.

**조치 필요**: 4곳 모두 `.2` 항목명을 `DC_Pack3 Comm Alm ...`으로, 변수명도 `DCpk3ComAlm`으로 정정.

---

## 3. 구조적 불일치 — `AlmEnb()` 배열 범위가 EEPROM 34항목을 초과

- `2.2.1SysCMDcode`의 Alarm Set Command Code 표는 정확히 **34항목**(`Pary(0,1)~Pary(0,34)`, 코드 2401~2434 = 실측 31항목 + Spare 3개)이며, **통신 Alarm(DC Pack Comm×3, Mux Comm)은 포함되어 있지 않음**.
- 그런데 `2.1StsBitInCall`의 ENB 표(H열)는 `AlmEnb(0)~AlmEnb(31)`(분석값 31항목, 맞음) 다음에 `AlmEnb(32)~(35)`를 통신 Alarm ENB(Pack1/2/3+Mux)에, `AlmEnb(36)~(39)`를 Spare2~5에 이어서 부여함. 결과적으로 이 시트가 암묵적으로 요구하는 EEPROM ENB 배열 크기는 40개(0~39)로, EEPROM 실제 항목 수(34개, MD 4절)와 불일치.
- 즉 **"통신 Alarm의 Enable/Disable을 EEPROM에 저장할 것인가"**가 두 자료 사이에서 정의가 갈림. MD/SysCMDcode 기준으로는 통신 Alarm이 EEPROM Pary 배열 밖에 있어야 하나, StsBitInCall 시트는 같은 배열 안에 번호를 매겨둠.
- 추가로 Latch 표(`PA_Sts(4)`)는 Spare를 13개(Spare1~13) 확보해뒀지만 EEPROM 쪽 Spare는 3개뿐이라, 나머지 10개는 애초에 EEPROM과 연결될 수 없는 구조. ENB 표가 그중 일부(Spare2~5)에만 `AlmEnb(36~39)`를 붙인 것도 일관성 없음(왜 Spare2~5만 EEPROM 연결이고 Spare6~13은 아닌지 근거 불명).

**권장**: MD 7절 "Alarm 항 정리 시 함께 확정할 것" 목록에 추가 —
① 통신 Alarm ENB/DIS를 EEPROM에 저장할지 여부(저장한다면 34항목 체계를 몇 항목으로 늘릴지, 안 한다면 StsBitInCall의 `AlmEnb(32~39)` 번호 삭제)
② Spare 비트 13개 중 EEPROM과 연결되는 것은 몇 개인지 명확화.

---

## 4. 사소한 항목

- `PA_Sts(3).13/.14`(Gain/Phase Volt Adjust)의 Latch 라벨에 다른 항목과 달리 `Alm Latch` 접미사가 빠져 있음. `2.2.1SysCMDcode`에는 정식 Level/Delay(6500/5400, Delay 10) 값이 있어 실제 Alarm 항목인 것은 맞으므로 라벨만 보완하면 됨.
- `PA_Sts(9)`(Unlatch2)의 bit 13·14는 공란인데, 같은 위치의 Latch(`PA_Sts(3).13/.14`)와 ENB(`PA_Sts(6).13/.14`)는 Gain/Phase Adjust volt로 채워져 있음 — Unlatch만 빠진 이유 확인 필요(의도적 생략인지 누락인지).
- `PaNolStstO`(row 61, Total Normal Status 변수명) — `PaTotFltO`, `PaSutDwnStsO` 등 다른 명명 규칙과 비교하면 `PaNorStsO`류의 오타로 보임. 기능 영향 없음.
- `2.2.1SysCMDcode`의 Spare 항목명이 `PA Spare`(32번) → `Spare 4`(33번) → `Spare 5`(34번)로 번호가 어긋나 있음(Spare1~3이 자연스러움) — MD가 이미 "Spare(항목 32~34) 항목명도 Spare1~3으로 정리 필요"로 짚어둔 부분과 일치, 재확인됨.

---

## 요약

MD 계획서의 0921 확정 내용과 `2.1StsBitInCall` 시트는 핵심 흐름(PAnal 임계값 사용, PA_Sts 비트 배치, Comm Alarm 비트 위치)에서 대체로 일치한다. 다만 **(1) Pack3 항목명이 4곳에서 Pack2로 잘못 복사된 실수**와 **(2) AlmEnb 배열이 EEPROM 34항목 범위를 넘어 통신 Alarm·추가 Spare까지 번호를 매겨 구조가 어긋나는 점**은 코딩 착수 전에 정리가 필요한 실질적 이슈로 보인다. MD의 "전체 미해결 항목" 목록(9번 원본 시트 정정, 8번 Alarm 항 정리)에 이 두 가지를 추가해 두는 것을 권장한다.
